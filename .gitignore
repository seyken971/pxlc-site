# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Astro
.astro

# Component library build output
dist-lib
dist-ce

# Design sync — staged scripts, build output, machine state
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Local tool / agent state — sauf la config Claude Code partagée
# (hooks, skills, settings.json), versionnée pour tous les worktrees
.claude/*
!.claude/hooks/
!.claude/skills/
!.claude/settings.json
.unlighthouse
CLAUDE.local.md
TASKS.md

# Plaquette : aperçus PNG locaux (npm run plaquette:previews) ; le PDF vit
# dans public/files/ et se committe
.previews/

# Snapshot SEO de travail (compare a docs/seo-baseline, regenere a chaque build)
docs/seo-current
